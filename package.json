{
  "name": "certbot-gui-tool",
  "version": "0.0.1",
  "author": "andrew",
  "description": "the gui tool for certbot",
  "main": "src/main.js",
  "scripts": {
    "electron-open": "electron src/main.js",
    "electron-build": "electron-builder --win --x64"
  },
  "devDependencies": {
    "electron": "9.0.4",
    "electron-builder": "^22.7.0",
    "electron-reload": "^1.5.0"
  }
}
